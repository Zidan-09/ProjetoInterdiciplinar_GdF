enum Priority {
    VeryPriority = 'very_priority',
    Priority = 'priority',
    NonPriority = 'non_priority'
}

enum TypeQueue {
    Recep = 'recep',
    Triage = 'triage',
    Consult = 'consult'
}

export { Priority, TypeQueue }